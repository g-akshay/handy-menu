/*!
 * Version - 1.0.0
 * Handy menu plugin
 * Configurable contextual menu
 */ 

(function($, window, undefined){

    $.fn.handyMenu = function(options){

        var defaults = {


        };

        var settings = $.extend( true, {}, defaults, options );

        return this.each(function() {
            // Plugin code would go here...
        });

    };


}(jQuery, window));